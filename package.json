{
  "name": "cloudflare-cortex-posture-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run types && npm run typecheck && npm run lint && npm test && npm run deploy:dry",
    "deploy": "npm run migrate:remote && wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "dev": "wrangler dev --test-scheduled",
    "lint": "oxlint --deny-warnings src test",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote",
    "cortex-test": "bash scripts/cortex-test.sh",
    "smoke": "bash scripts/smoke-test.sh",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "cloudflare": {
    "bindings": {
      "ACCESS_TEAM_DOMAIN": {
        "description": "Your Zero Trust team domain, e.g. `https://myteam.cloudflareaccess.com`. Used to authenticate requests to this Worker's API."
      },
      "ACCESS_AUD": {
        "description": "AUD of the Access application protecting this Worker."
      },
      "CORTEX_BASE_URL": {
        "description": "Cortex XDR API HTTPS origin. See the README's Cortex Configuration section."
      },
      "CORTEX_API_KEY": {
        "description": "Cortex XDR API key with endpoint-read access. See the README's Cortex Configuration section."
      },
      "CORTEX_API_KEY_ID": {
        "description": "Cortex XDR API key ID from the API Keys page."
      },
      "CLOUDFLARE_API_TOKEN": {
        "description": "Cloudflare API token scoped to the account with **Zero Trust Write**. Also powers the dashboard's list selector."
      }
    }
  },
  "dependencies": {
    "jose": "^6.2.10"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "oxlint": "^1.80.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.126.0"
  }
}
